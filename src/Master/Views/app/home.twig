{% extends 'layout.twig' %}

{% block body %}

    <div class="container">
        <div class="jumbotron">
            <h1>{{title}}</h1>
            {{message}}
        </div>
    </div>

{% endblock %}
